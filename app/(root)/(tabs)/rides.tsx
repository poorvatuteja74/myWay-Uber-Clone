import { Text, View } from "react-native";
import { SafeAreaView } from "react-native";

const Rides = () => {
  return (
    <SafeAreaView>
      <Text>Ride</Text>
    </SafeAreaView>
  );
};
export default Rides;
