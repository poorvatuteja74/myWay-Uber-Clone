import { Text, View } from "react-native";
import { SafeAreaView } from "react-native";

const Chat = () => {
  return (
    <SafeAreaView>
      <Text>Chat</Text>
    </SafeAreaView>
  );
};
export default Chat;
