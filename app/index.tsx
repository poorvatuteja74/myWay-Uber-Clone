import { Text, View } from "react-native";
import { SafeAreaView } from "react-native";

const Home = () => {
  return (
    <SafeAreaView>
      <Text>Home</Text>
    </SafeAreaView>
  );
};
export default Home;
