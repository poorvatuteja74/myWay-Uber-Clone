import { Text, View } from "react-native";
import { SafeAreaView } from "react-native";

const SignIn = () => {
  return (
    <SafeAreaView>
      <Text>Sign-in</Text>
    </SafeAreaView>
  );
};
export default SignIn;
