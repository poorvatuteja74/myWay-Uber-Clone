import { Text, View } from "react-native";
import { SafeAreaView } from "react-native";

const Onboarding = () => {
  return (
    <SafeAreaView>
      <Text>Welcome</Text>
    </SafeAreaView>
  );
};
export default Onboarding;
